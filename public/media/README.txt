Place your background GIF here as:

  rog-neon.gif

This folder is referenced by CSS class `rog-neon-bg`.
The path used in code is `/media/rog-neon.gif`.

Tip: Prefer a 1920x1080 or higher looping GIF/WebM for quality.
